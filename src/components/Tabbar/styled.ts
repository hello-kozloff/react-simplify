import styled from 'styled-components';

/**
 * The wrapper of tabbar.
 */
export const Wrapper = styled.div`

`;

export const Navigation = styled.div`
    
`;

/**
 * The wrapper of content.
 */
export const Content = styled.div`

`;
